# Runtime dependencies (pip-installable)
python-debian
requests

# Optional: Launchpad PPA availability checks (lazy import — app works without it)
launchpadlib

# PyGObject (GTK4/libadwaita) is a system package — install via apt, not pip:
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-4.0 gir1.2-adw-1

# Development / testing
pytest
pytest-cov
pytest-mock
