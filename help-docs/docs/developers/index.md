# Developer API Reference

The API reference is generated by Sphinx from the source code and merged into the
deployed site at `/developers/`. On the live site, this page is replaced by the
Sphinx output.

To browse it locally:

```bash
pip install sphinx
PYTHONPATH=src sphinx-build sphinx sphinx/_build/html
# Then open sphinx/_build/html/index.html
```
