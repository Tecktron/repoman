name: Build and attach .deb

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to attach the .deb to (e.g. 0.1.5)"
        required: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update -q
          sudo apt-get install -y \
            build-essential \
            debhelper \
            dh-python \
            pybuild-plugin-pyproject \
            python3-all \
            python3-setuptools

      - name: Build .deb
        run: dpkg-buildpackage -us -uc

      - name: Attach to release
        run: gh release upload "${{ inputs.tag || github.ref_name }}" ../repoman_*.deb ../repoman_*.buildinfo ../repoman_*.changes
        env:
          GH_TOKEN: ${{ github.token }}
